name: Regression Tests Execution

on:
  workflow_dispatch:
    inputs:
      env:
        description: 'Test environment'
        default: 'qa'
      browser:
        description: 'Browser to run tests'
        default: 'chromium'
      headless:
        description: 'Headless mode'
        default: 'true'
      threadcount:
        description: 'Parallel threads'
        default: '1'
      tag:
        description: 'Cucumber tags'
        default: '@regression'

jobs:
  regression-job:
    runs-on: ubuntu-latest

    steps:
      # ─────────────────────────────────────────
      # 1️⃣  Checkout main branch
      # ─────────────────────────────────────────
      - name: Checkout Main
        uses: actions/checkout@v4

      # ─────────────────────────────────────────
      # 2️⃣  Setup Java 21
      # ─────────────────────────────────────────
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      # ─────────────────────────────────────────
      # 3️⃣  Install Allure CLI
      # ─────────────────────────────────────────
      - name: Install Allure CLI
        run: npm install -g allure-commandline

      # ─────────────────────────────────────────
      # 4️⃣  Install Playwright browsers for Java
      #      The Java Playwright lib manages its own
      #      browser binaries — must install via mvn
      # ─────────────────────────────────────────
      - name: Install Playwright Browsers
        run: mvn exec:java -e -D exec.mainClass=com.microsoft.playwright.CLI -D exec.args="install --with-deps" -D exec.classpathScope=test

      # ─────────────────────────────────────────
      # 5️⃣  Restore Allure history from gh-pages
      # ─────────────────────────────────────────
      - name: Checkout gh-pages for History
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          path: gh-pages
        continue-on-error: true

      - name: Restore Regression History
        run: |
          rm -rf target/allure-results
          mkdir -p target/allure-results/history
          if [ -d gh-pages/allure-history/regression/history ]; then
            echo "✅ Prior regression history found — restoring"
            cp -r gh-pages/allure-history/regression/history/. target/allure-results/history/
          else
            echo "ℹ️  No prior regression history — first run"
          fi

      # ─────────────────────────────────────────
      # 6️⃣  Run Regression Tests
      #      continue-on-error: true — regression is
      #      non-blocking by policy
      # ─────────────────────────────────────────
      - name: Run Regression Tests
        id: run
        continue-on-error: true
        run: |
          xvfb-run --auto-servernum -- mvn clean test \
            -Dcucumber.filter.tags="${{ github.event.inputs.tag || '@regression' }}" \
            -Denv="${{ github.event.inputs.env || 'qa' }}" \
            -Dbrowser="${{ github.event.inputs.browser || 'chromium' }}" \
            -Dheadless="${{ github.event.inputs.headless || 'true' }}" \
            -Dthreadcount="${{ github.event.inputs.threadcount || '1' }}" \
            -Dparallel.thread.count="${{ github.event.inputs.threadcount || '1' }}" \
            -Dallure.results.directory=target/allure-results \
            -Dsurefire.systemPropertyVariables.env="${{ github.event.inputs.env || 'qa' }}" \
            -Dsurefire.systemPropertyVariables.browser="${{ github.event.inputs.browser || 'chromium' }}" \
            -Dsurefire.systemPropertyVariables.headless="${{ github.event.inputs.headless || 'true' }}" \
            -Dsurefire.systemPropertyVariables.cucumber.filter.tags="${{ github.event.inputs.tag || '@regression' }}"

      # ─────────────────────────────────────────
      # 7️⃣  Debug — verify outputs after test run
      #      Remove this step after first clean run
      # ─────────────────────────────────────────
      - name: Debug — Verify Output Paths
        if: always()
        run: |
          echo "====== target/allure-results ======"
          ls -la target/allure-results/ || echo "❌ NOT FOUND"
          echo "====== environment.properties ======"
          cat target/allure-results/environment.properties || echo "❌ NOT FOUND"
          echo "====== test-output ======"
          find . -path ./gh-pages -prune -o -name "failedLocators_*.json" -print || echo "❌ no failedLocators found"
          echo "====== defect-age-report ======"
          find . -path ./gh-pages -prune -o -name "defect-age-report.csv" -print || echo "❌ not found"
          echo "====== surefire-reports ======"
          ls target/surefire-reports/ || echo "❌ NOT FOUND"

      # ─────────────────────────────────────────
      # 8️⃣  Generate Defect Age Report
      # ─────────────────────────────────────────
      - name: Generate Defect Age Report
        if: always()
        run: |
          mvn exec:java \
            -Dexec.mainClass="com.samtech.qa.testutilities.AllureDefectAge" \
            -Dexec.classpathScope=test \
            -Denv="${{ github.event.inputs.env || 'qa' }}" \
            -Dbrowser="${{ github.event.inputs.browser || 'chromium' }}" \
            -Dheadless="${{ github.event.inputs.headless || 'true' }}"
          echo "====== defect-age-report.csv ======"
          cat target/defect-age-report.csv || echo "❌ NOT generated"

      # ─────────────────────────────────────────
      # 9️⃣  Generate Allure HTML Report
      # ─────────────────────────────────────────
      - name: Generate Allure Report
        if: always()
        run: |
          allure generate target/allure-results -o allure-report-regression --clean
          echo "====== allure-report-regression/history ======"
          ls allure-report-regression/history/ || echo "❌ history missing from report"

      # ─────────────────────────────────────────
      # 🔟  Save Regression History to gh-pages FIRST
      #      Do this BEFORE peaceiris deploy to avoid
      #      racing two simultaneous gh-pages writes
      # ─────────────────────────────────────────
      - name: Save Regression History to gh-pages
        if: always()
        run: |
          rm -rf gh-pages
          git clone --branch gh-pages --single-branch \
            https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git \
            gh-pages || (mkdir -p gh-pages && cd gh-pages && git init && git remote add origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git)
          mkdir -p gh-pages/allure-history/regression
          rm -rf gh-pages/allure-history/regression/history
          cp -r allure-report-regression/history gh-pages/allure-history/regression/history
          cd gh-pages
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add allure-history/regression/history
          git diff --cached --quiet \
            && echo "ℹ️  No history changes" \
            || git commit -m "chore(regression): update allure history [skip ci]"
          git push origin gh-pages
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ─────────────────────────────────────────
      # 1️⃣1️⃣  Deploy Regression Report to GitHub Pages
      #      keep_files: true preserves smoke-report
      # ─────────────────────────────────────────
      - name: Deploy Regression Report to GitHub Pages
        if: always()
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./allure-report-regression
          destination_dir: regression-report
          keep_files: true

      # ─────────────────────────────────────────
      # 1️⃣2️⃣  Upload ALL four artifacts
      # ─────────────────────────────────────────
      - name: Upload Regression Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: regression-artifacts
          if-no-files-found: warn
          path: |
            target/allure-results/
            allure-report-regression/
            target/defect-age-report.csv
            test-output/failedLocators_*.json

      # ─────────────────────────────────────────
      # 1️⃣3️⃣  Report outcome — non-blocking
      #         Regression failures are visible but
      #         do not block the pipeline
      # ─────────────────────────────────────────
      - name: Report Regression Outcome
        if: always()
        run: |
          if [ "${{ steps.run.outcome }}" = "failure" ]; then
            echo "⚠️  Regression tests FAILED — check the report"
            echo "ℹ️  Pipeline continues — regression is non-blocking"
          else
            echo "✅ All regression tests passed"
          fi